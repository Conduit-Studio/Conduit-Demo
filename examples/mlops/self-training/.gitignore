# Local run artifacts — regenerated by scripts/run_loop_local.py (and Conduit in-cloud).
runs/
# Generated CIFAR-10 splits + the raw download (regenerated by prepare_cifar_subset.py).
# Everything under data/ is generated EXCEPT data/conduit/ — the small, tracked Verify fixtures.
/data/*
!/data/conduit/
# Saved model checkpoints.
*.pt
# Local virtualenv (also covered by the repo-root .gitignore).
.venv/
