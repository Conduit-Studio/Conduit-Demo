"""Shared logic for the YOLO fine-tune sweep example.

Imported by the Conduit Run Code entrypoint (../select_best.py) and by the tests.
Pure Python, no AWS — so the selection logic is unit-testable off a laptop.
"""
