{
  "_comment": "The Config·JSON node's value. `trials` is the sweep grid the Map fans out over (one SageMaker training job per entry); each trial's keys become that job's hyperparameters. `dataset` is the shared input every trial trains on. Swap `dataset.bucket` for your bucket; pin the training image by @sha256 digest in the Train Model node (see scripts/build_and_push.sh).",
  "metric": "mAP50-95",
  "dataset": {
    "bucket": "conduit-demo-data",
    "key": "examples/mlops/yolo-finetune/data/dataset/"
  },
  "trials": [
    { "name": "n-640",  "weights": "yolov8n.pt", "imgsz": 640,  "lr0": 0.01,  "epochs": 50 },
    { "name": "s-640",  "weights": "yolov8s.pt", "imgsz": 640,  "lr0": 0.01,  "epochs": 50 },
    { "name": "s-1280", "weights": "yolov8s.pt", "imgsz": 1280, "lr0": 0.005, "epochs": 80 },
    { "name": "m-640",  "weights": "yolov8m.pt", "imgsz": 640,  "lr0": 0.01,  "epochs": 50 }
  ]
}
