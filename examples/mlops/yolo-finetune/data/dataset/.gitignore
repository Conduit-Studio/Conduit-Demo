# Filled by scripts/download_coco_subset.py + scripts/convert_coco_to_yolo_bbox.py
# (real COCO person+car images + YOLO labels) — don't commit those.
# data.yaml (the static shape) IS committed.
images/
labels/
