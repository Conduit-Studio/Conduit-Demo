# YOLO Fine-tune Sweep (+ eval + lineage)

See [example.md](example.md) for the full Conduit Deploy workflow: the sweep grid, the BYO training container, building the canvas, deploying the Step Functions sweep, the eval gate, and how each registered model gets a reproducible lineage record.
