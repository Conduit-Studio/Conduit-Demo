# Local run artifacts — regenerated by scripts/run_sweep_local.py (and Conduit in-cloud).
runs/
# Pretrained + downloaded YOLO weights (ultralytics fetches these on demand).
*.pt
# Local virtualenv (also covered by the repo-root .gitignore).
.venv/
